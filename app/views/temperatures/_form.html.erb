<div class="container-fluid">
<%= form_with(model: temperature, local: true) do |form| %>
  <% if temperature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(temperature.errors.count, "error") %> prohibited this temperature from being saved:</h2>

      <ul>
      <% temperature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <form>
  <div class="form-group">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :temperature_time, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :date %>
    <%= form.date_select :date, id: :temperature_date, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :minimum_temperature %>
    <%= form.text_field :minimum_temperature, id: :temperature_minimum_temperature, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :maximum_temperature %>
    <%= form.text_field :maximum_temperature, id: :temperature_maximum_temperature, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
</form>
</div>
